meta {
  name: Delete Asset
  type: http
  seq: 3
}

delete {
  url: {{baseUrl}}/assets/{{assetId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

vars:pre-request {
  assetId: 1
}
